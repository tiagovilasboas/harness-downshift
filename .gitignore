# Binaries (compiled output at repo root or /tmp), not the cmd/downshift source
/downshift
downshift-bin
*.exe
*.dll
*.so
*.dylib
dist/

# Test
*.out
coverage.html
coverage.out

# Go
go.work
go.work.sum

# IDE / OS
.DS_Store
.idea/
.vscode/
*.swp

# Local
*.log

# Catalog overrides — the user's ~/.harness-downshift/catalog.json and any
# project-level override are personal data, NOT committed to the repository.
# The embedded default catalog (internal/catalog/catalog.json) IS committed —
# it ships inside the binary via go:embed.
# Override path: ~/.harness-downshift/catalog.json (auto-loaded at runtime).
/catalog.json
.harness-downshift/
