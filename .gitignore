# Binaries (compiled output at repo root or /tmp), not the cmd/downshift source
/downshift
downshift-bin
*.exe
*.dll
*.so
*.dylib
dist/

# Test
*.out
coverage.html
coverage.out

# Go
go.work
go.work.sum

# IDE / OS
.DS_Store
.idea/
.vscode/
*.swp

# Local
*.log

# Catalog — the curated catalog.json with tiered model data is a strategic
# asset and is intentionally NOT published in this repository.
# Use catalog.sample.json as a starting point, then place your filled-in
# catalog at ~/.harness-downshift/catalog.json (loaded automatically).
# Any project-level override also stays local:
catalog.json
.harness-downshift/
